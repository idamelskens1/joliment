---
import { Image } from "astro:assets";
import MainLayout from "../layouts/MainLayout.astro";
import Button from "../components/Button.astro";
import bigbow from "../assets/bigbow.svg";
import greenbow from "../assets/greenbowclose.svg";
import smallbow from "../assets/smallbow.svg";
import betaling from "../assets/betaling.svg";

---

<MainLayout>
  <section>


    <section class="grid gap-10 lg:grid-cols-2 px-8 py-10 mt-50">

  <!-- Venstre kolonne -->
  <div class="space-y-6">
    <!-- stort billede -->
    <Image src={bigbow} alt="Stor sløjfe" class="w-full h-auto" />

    <!-- to små billeder i grid -->
    <div class="grid grid-cols-2 gap-4">
      <Image src={smallbow} alt="Lille sløjfe" class="w-full h-auto" />
      <Image src={greenbow} alt="Closup" class="w-full h-auto" />
    </div>
  </div>

  <!-- højre kolonne med teksten og knapperne -->
  <div class="flex flex-col gap-4">
    <!-- købs-teksten er her under -->
     <h2>Sløjfe</h2>
     <p>Når du køber en sløjfe, er det en hyggelig overraskelse, hvilken en der gemmer sig i æsken. Én ting er dog sikker: 
Dit køb hjælper et hospitalsramt barn med at få deres juleønske opfyldt gennem Make-A-Wish.</p>
<p>25% af salget går til Make A Wish</p>
<p>En stor sløjfe måles til at være x i højde og bredde. En lille måles til at være x i højde</p>
<div class="flex flex-col">
  <div class="grid grid-cols-3 sm:grid-cols-3 mt-10 lg:mt-30 gap-5">
    <Button variant="tertiary" size="small">LILLE</Button>
    <Button variant="tertiary">STOR</Button>
  </div>
<a href="/tak"><Button variant="primary">Køb sløjfe</Button></a>
<Image src={betaling} alt="Closup" class="w-30 h-auto" />
  </div>

</section>









</MainLayout>
